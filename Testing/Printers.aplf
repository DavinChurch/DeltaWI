 Printers
 '#'∆WI Pass MT⊢'Reset' ⍝ Clean out any previous GUI work

 '#'∆WI Pass{2=1↓⍴⍵}⊢'Printers'
 '#'∆WI Pass{(1=≢⍴⍵)∧(2=≢⍵)∧2=≡⍵}⊢'Printers' 'Default'
 '#'∆WI Fail'Printer change default operation cannot be supported'⊢'Printers' 'Default' 'xyz'
 '#'∆WI Fail'Printer tree operation cannot be supported'⊢'Printers' 'Tree'

 '#'∆WI Pass MT⊢'Reset' ⍝ Clean up after tests
