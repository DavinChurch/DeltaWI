 result←object value arg
⍝ Allow reading and writing the Form's return value from Wait

 :If 0=≢arg
     result←object ⎕VGET⊂'_value' 0
 :Else
     :If 1=≢arg ⋄ arg←⊃arg ⋄ :EndIf ⍝ Treat a single value as unnested
     object._value←arg ⍝ New value to be stored
     result←NoRes
 :EndIf
