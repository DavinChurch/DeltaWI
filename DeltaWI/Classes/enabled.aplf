 result←object enabled arg
⍝ Allow making an object "active" or "inactive"

 :If ×≢arg ⍝ If we have a new state, change it
     'DOMAIN ERROR'##.##.Error(1≠≢arg)∨~0 1∊⍨1↑arg
     object._enabled←⊃arg
     ⍝object ##.∆WSet'Active'(~arg)
     ⍝? Disabling a GUI object while leaving it open isn't possible in Dyalog
     result←NoRes
 :Else ⍝ Otherwise just read it
     result←object._enabled
 :EndIf
