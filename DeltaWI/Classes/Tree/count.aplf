 result←object count arg;lsize;csize

 ⍝ [1]←Total # of nodes in the tree
 ⍝ [2]←Number of nodes that will fit on the screen (Dyalog cannot provide this number so try to simulate it)

 '∆WI ACTION ERROR: Property "count" is read-only'##.##.Error×≢arg
 result←≢object ⎕VGET⊂'_list'(0 5⍴0)
 lsize←⊃object ##._supp.GetTextSize'Yy' ⋄ csize←⊃object ##.size ⍬
⍝? We are not getting the right answer back, so artificially adjust it downwards a little bit so it will pass our comparison tests
 result,←⌊¯0.2+csize÷lsize ⍝ Do our best approximation of how many full lines will fit on the screen
