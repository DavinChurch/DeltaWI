 result←object bin arg;pbins;devmode
 pbins←object ##.##.∆WG'PaperSources'
 :If 0=≢arg
     result←object ⎕VGET⊂'_bin'(×≢pbins)
 :Else
     arg←##.##.Single arg
     '∆WI DOMAIN ERROR: bin'##.##.Error~##.##.IsInt arg
     '∆WI LENGTH ERROR: bin'##.##.Error 1≠≢arg
     '∆WI DOMAIN ERROR: bin'##.##.Error 0>arg←⊃arg
     object._bin←arg←⊃1,⍨arg∩⍳≢pbins
     object ##.##.∆WS⊂'PaperSource'(arg⊃pbins)
     devmode←object DevMode ⍬ ⋄ devmode[56+⍳2]←80 ⎕DR⊃(163,⍨⎕DR arg)⎕DR arg ⋄ object._devmode←devmode ⍝ Update DevMode to match
     result←##.NoRes
 :EndIf
