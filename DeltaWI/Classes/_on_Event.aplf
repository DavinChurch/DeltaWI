 result←run _on_Event message
⍝ Execute an 'Event' method handler
⍝ ∆WSELF, ∆WEVENT, ∆WARG, & ∆WRES have already been pre-set to the requested values.
⍝ Nothing is in "message" - everything has already been dealt with.
⍝ The result (actual data, not "ok" code) is returned directly from the method call.

 result←##.Run run
