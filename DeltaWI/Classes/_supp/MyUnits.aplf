 (tunits punits units)←MyUnits object;units;ppxsize;pscale;scale;pxsize
⍝ Determine what pixel-size my object's (and my parent's, and the system's) unit sizes are

 (scale pxsize)←object ⎕VGET('_scale' 1)('_pix_size'(0 0))
 (pscale ppxsize)←(⊃object._Ancestry)⎕VGET('_scale' 1)('_pix_size'(0 0))
 units←##.##.GUIroot._units ⍝ Get system conversion factors
 tunits←(units⍪⍨pxsize÷1⌈|2↑3↓scale)[1+⊃scale;] ⍝ Compute unit-conversions for this object's scale ⍝? Is this anywhere close to right for scale=0??
 punits←(units⍪⍨ppxsize÷1⌈|2↑3↓pscale)[1+⊃pscale;] ⍝ Compute unit-conversions for the parent object's scale

 ⍝? Note: We might need to use ∆Border for some of these calculations?? Or won't it matter?
