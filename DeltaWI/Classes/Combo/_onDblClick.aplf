 ok←run _onDblClick message;style;object
 object←⊃message
 style←##.##.BitVals object ⎕VGET⊂'_Style' 0
 :If 1∊style ⍝ This only runs when 1∊style, so we'll ignore it otherwise
     ##.##.Run run ⍝ No arguments or results are used for this event.
 :EndIf
 ok←1
