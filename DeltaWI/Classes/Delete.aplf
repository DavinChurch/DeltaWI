 result←object Delete arg;hidden;kids;mybuddy;support
⍝ Delete a GUI object (we're already sure it's here)

 '∆WI ACTION ERROR: Method "Delete" does not accept an argument'##.Error×≢arg
 :If ×≢kids←object children ⍬
     result←(object ##.Locate¨##.LeafName¨kids)('Delete'##.Invoke)¨⊂⍬ ⍝ Recursively delete all my immediate children
 :EndIf

 :Section ⍝ Make sure it's closed first
     :If 0≠object._opened ⍝ If open, we'll need to change it to closed first
         :If object._visible>0
             object._visible←0 ⍝ Just in case something wonders about this before it's gone
             object ##.∆WSet⊂'Visible' 0
             'Hide'##.Event object ##.Application ⍝ Run the onHide event handler
         :EndIf
         object.(_opened _enabled)←0 0 ⍝ Just in case something wonders about this before it's gone (or after it's a ghost)
         object ##.∆WSet⊂'Active' 0
         'Destroy'##.Event object ##.Application ⍝ Run the onDestroy event handler
     :EndIf
 :EndSection

 :Section ⍝ Clean out any Spinner-buddies
     :If 9=object.⎕NC'_buddy_' ⍝ Check the primary buddy-connection signal
     :AndIf ⎕NULL≢mybuddy←object._buddy_ ⍝ Who am I connected to?
         :If object._class≡'Spinner' ⍝ If I'm the spinner being deleted
             ⍝ (I don't need to disconnect myself at the original name level because I'm going away)
             {}mybuddy where,⊂mybuddy._where_buddy ⋄ mybuddy.⎕EX'_where_buddy' ⍝ Restore my buddy's original 'where' & disconnect
         :Else ⍝ Then I must be the buddy itself
             mybuddy._buddy←'' ⍝ Disconnect him at the original name level so he no longer tries to point to me
             {}mybuddy where,⊂mybuddy._where_orig ⋄ mybuddy.⎕EX'_where_orig' ⍝ Restore the spinner's original 'where' & disconnect
         :EndIf
         ⍝ (I don't need to restore my own 'where' because I'm going away)
         mybuddy.⎕EX'_buddy_' ⍝ Disconnect his logical cross-connection to me
         object.⎕EX'_buddy_' ⍝ Disconnect my own logical cross-connection to him
⍝? Should we check here to make sure that the cross-connected pointers have removed the appropriate objects from ⎕WN?
     :EndIf
 :EndSection

 :Section ⍝ Get rid of hidden objects
     hidden←'_mb' '_mdi' '_pic'
     :For hidden :In (9=object.⎕NC↑hidden)/hidden ⍝ See about getting rid of any hidden Dyalog immediate children
         :Select hidden
         :Case '_mb'
⍝? Add any special handling for this
         :Case '_mdi'
⍝? Add any special handling for this
         :Case '_pic'
⍝? Add any special handling for this
         :EndSelect
         :If ×≢support←'_'(object ⎕VGET hidden).⎕NL ¯9 ⍝ This may have (mostly for Pictures) other hidden Dyalog (drawing) objects contained within it
         :AndIf ×≢support←(∧/¨(1↓¨support)∊¨⊂⎕D)/support ⍝ Look for _999-style native objects
             ##.∆EX⍕¨object ⎕VGET support
         :EndIf
         ##.∆EX⍕object ⎕VGET hidden
     :EndFor
     :If ×≢support←'_'object.⎕NL ¯9 ⍝ Other hidden Dyalog objects
     :AndIf ×≢support←(∧/¨(1↓¨support)∊¨⊂⎕D)/support ⍝ Look for _999-style native objects
         ##.∆EX⍕¨object ⎕VGET support
     :EndIf
 :EndSection

 'Delete'##.Event object ##.Application ⍝ Run the onDelete event handler

 _supp.waitoff object ⍝ Clean up any waiting-form situations

⍝? Do we need to specially handle any current page data before deleting a Printer object?
⍝? Anything else to clean up?

⍝ Finally, time to actually get rid of it!
 ##.∆EX⍕object

 result←NoRes ⍝ Never return a result
