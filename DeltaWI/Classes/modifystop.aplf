 result←object modifystop arg
⍝ Mark an object to stop cascading Modify events upwards to its parent

 :If 0=≢arg
     result←object ⎕VGET⊂'_modifystop' 0
 :Else
     'DOMAIN ERROR'##.Error(1≠≢arg)∨~0 1∊⍨1↑arg
     object._modifystop←##.Single arg
     result←NoRes
 :EndIf
