 result←object selindents arg;para;t;tunits;units;pix_twip
⍝ Paragraphs might have left or right indentations; detect or turn on/off
⍝ Use properties directly rather than an APL variable
⍝? Cannot support arg[2] = Bullet indent amount

 para←object ##.##.∆WG'ParaFormat'
 (tunits units)←1 3⊂⍛⌷##._supp.MyUnits object
 pix_twip←units[4;] ⍝ Pixels per twip, for converting scales

 :If 0=≢arg
     result←1 0 1\para[2 4]×2⊃pix_twip÷tunits ⍝? Left and right indents only, converted to scale-units
 :Else
     arg←##.##.Single arg ⍝ Make sure a nested singleton is flattened
     '∆WI LENGTH ERROR: selindents'##.##.Error 3<≢arg
     '∆WI DOMAIN ERROR: selindents'##.##.Error~∧/arg∊¨⊂0 1 ⍬
     arg←(3↑arg,3⍴⊂⍬)÷2⊃pix_twip÷tunits ⍝ Convert scale-units to twips
⍝? Report unsupported use of bullet-indents in [2]?
     t←0=≢¨arg ⋄ (t/arg)←t/1 0 1\para[2 4] ⍝ Assign ⍬ defaults
     para[2 4]←arg[1 3] ⋄ para[3]←0
     object ##.##.∆WS⊂'ParaFormat'para
     result←##.NoRes
 :EndIf
