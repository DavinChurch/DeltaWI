 result←object onClose arg
 ⍝ Important: Closing Dyalog Forms/SubForms deletes them by default, so the event processor needs to always return 0 to deny that for us
 result←object('Close'##.##.SetEvent'Close')arg
