 result←object children arg;hidden;at;here
⍝ Produce a list of available children belonging to this object

 '∆WI ACTION ERROR: Property "children" is read-only'##.Error×≢arg
 :If 0=≢result←1↓(⊂''),⍕¨##.∆WN object ⋄ :Return ⋄ :EndIf
 :While ×≢hidden←'_mb' '_mdi' '_pic'∩⍨##.LeafName¨result ⍝ Search under known-invisible children
     at←(##.LeafName¨result)⍳⊂hidden←⊃hidden
     here←1↓(⊂''),⍕¨##.∆WN object ⎕VGET hidden
     result←((at-1)↑result),here,at↓result
 :EndWhile
 result/⍨←~##.IsPrivate¨result ⍝ Eliminate numbered invisible children
 result←##.LeafName¨result ⍝ Don't include any prefixes of these names
 result,⍨¨←⊂{⍵↓⍨2×'#.'≡2↑⍵}object._self,'.' ⍝ Result must return full APL+Win GUI names (including APL+Win parentage), except '#.'
 result←1↓''⊂⍛,result ⍝ Make sure empty list has the right prototype
